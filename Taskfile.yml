# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

tasks:
  format:
    desc: Format all files
    cmds:
      - oxfmt && nixfmt **/*.nix && ruff format --line-length 120 **/*.py
    silent: true
